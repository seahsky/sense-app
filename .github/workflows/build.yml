name: Build

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Build CindyApp (embeds CindyWatch)
        run: |
          xcodebuild build \
            -project Cindy.xcodeproj \
            -scheme CindyApp \
            -destination 'generic/platform=iOS Simulator' \
            CODE_SIGNING_ALLOWED=NO

      - name: Find an iOS Simulator
        id: sim
        run: |
          UDID=$(xcrun simctl list devices available -j | python3 -c '
          import json, sys
          devices = json.load(sys.stdin)["devices"]
          for runtime, entries in devices.items():
              if "iOS" not in runtime:
                  continue
              for d in entries:
                  print(d["udid"])
                  sys.exit(0)
          sys.exit(1)
          ')
          echo "udid=$UDID" >> "$GITHUB_OUTPUT"

      - name: Run CindyKit unit tests
        # Plain `swift test` builds for the host (macOS) by default, but CindyKit
        # imports WatchConnectivity, which macOS does not have — so the tests need
        # an iOS Simulator destination.
        #
        # Run through the project's CindyKitTests target rather than the package's
        # own scheme. Testing a bare Package.swift makes xcodebuild infer supported
        # platforms for an implicit workspace, which fails intermittently with
        # "Supported platforms for the buildables in the current scheme is empty"
        # and then offers no simulator destination at all. The project target uses
        # the same destination resolution as every other target here.
        run: |
          xcodebuild test \
            -project Cindy.xcodeproj \
            -scheme CindyKitTests \
            -destination "id=${{ steps.sim.outputs.udid }}"
